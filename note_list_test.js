
  function testNoteListIsTakesArrayOfNotes() {
    var noteList = new NoteList();
    assert.isTrue(noteList.add("test") !== []);
  };
  testNoteListIsTakesArrayOfNotes();

  function testNoteListIsTakesArrayOfNotes() {
    var noteList = new NoteList();
    assert.isTrue(noteList.returnMethod() !== []);
  };
  testNoteListIsTakesArrayOfNotes();
